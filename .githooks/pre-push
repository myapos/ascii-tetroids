#!/bin/sh
# Pre-push hook: Run tests and lint before pushing

echo "🧪 Running tests..."
npm test -- --run
if [ $? -ne 0 ]; then
  echo "❌ Tests failed. Fix errors before pushing."
  exit 1
fi

echo "🔍 Running linter..."
npm run lint
if [ $? -ne 0 ]; then
  echo "❌ Lint failed. Fix errors before pushing."
  exit 1
fi

echo "✅ All checks passed. Pushing..."
exit 0
