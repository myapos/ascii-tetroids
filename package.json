{
  "name": "ascii-tetris",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "tsx index.ts",
    "debug": "node --inspect --import tsx index.ts",
    "start": "tsx index.ts",
    "build": "esbuild index.ts --bundle --platform=node --format=esm --outdir=dist --external:chalk",
    "type-check": "tsc --noEmit",
    "test": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "esbuild": "^0.27.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "chalk": "^5.6.2"
  }
}
